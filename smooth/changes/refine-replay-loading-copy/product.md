# 产品需求

## 背景

公开回放加载时显示“正在读取权威数据…”，措辞偏技术实现，普通用户不容易立刻理解正在等待什么。

## 目标

回放加载态直接说明正在加载对局记录，降低技术术语感，并保持中英文语义一致。

## 范围

- 中文回放加载文案改为“正在加载对局记录…”。
- 英文回放加载文案改为“Loading match record…”。
- 只影响公开回放页面，不改变竞技场、榜单、指挥中心等页面的通用加载文案。

## 验收标准

- 回放主状态和战场占位区都使用回放专用加载文案。
- 中英文页面均显示对应语言的文案。
- 不改变回放请求、重试、错误处理或渲染流程。

