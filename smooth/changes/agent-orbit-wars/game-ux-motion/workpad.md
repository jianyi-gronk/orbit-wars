# 工作台

## 计划

- [x] 盘点当前首页、顶栏、动效与响应式边界。
- [x] 提炼参考产品的单一主行动、三步玩法和分幕叙事节奏。
- [x] 设计首页场景状态、导航菜单和全局点击反馈边界。
- [x] 实现并完成中英文、滚轮、键盘、点击和窄屏验证。

## 验收标准

- 顶栏一级业务入口不超过三个，次级内容仍可到达。
- 四幕首页支持滚轮、触控、键盘与直接场景跳转。
- 场景切换和点击反馈尊重 reduced motion，不阻塞业务动作。
- 中英文桌面与 390px 均无横向溢出。

## 验证

- `pnpm check`：通过；Web 10 个测试文件、24 项测试通过，Python 162 通过、5 跳过。
- `pnpm --filter @orbit-wars/web build`：通过；Next.js 生产构建与静态页面生成完成。
- 浏览器实测中文/英文首页：滚轮从 0→1、1→0；PageDown 从 0→1、Home 从 1→0。
- 场景指示器与任务菜单点击均产生能量脉冲，任务菜单展示 5 个次级入口。
- 390×844 四幕均可达，`scrollWidth === clientWidth === 390`，主 CTA 与三入口导航可用。
- reduced-motion 由运行时 `matchMedia`、CSS media query 与质量测试共同守护，关闭滚轮接管和装饰动画。

## 备注

- 外部参考站的可视浏览器连接不稳定；已通过公开首页内容确认其单一主行动、三步流程和分段内容结构。
- 视觉必须保持 Orbit/Wars 原创轨道战争表达，不复用坦克品牌资产。

## 疑问

- 已决策：桌面滚轮采用相邻场景推进；触控保持原生滚动并用 snap 对齐。
- 已决策：历史和 Agent Guide 收进任务菜单，不删除路由。
