# 工作台

## 计划

- [x] 调研 HTML-in-Canvas、Three.js 与当前首页链路
- [x] 明确产品目标、范围与验收标准
- [ ] 完成技术设计与任务拆分
- [ ] 实现首页共享轨道世界与转场
- [ ] 验证双语、交互、降级、性能与构建

## 验收标准

- 四幕共享一个可感知连续的轨道世界，场景切换会改变空间状态。
- 首屏主行动清晰，中英文、键盘、触控和移动端可用。
- reduced-motion 与无 WebGL 均有完整降级。
- 自动化检查、类型检查和生产构建通过。

## 验证

- 运行首页交互单元测试、web lint、typecheck 与 build。
- 在浏览器检查 `/zh` 与 `/en` 的桌面首屏、四幕切换和控制台。
- 使用窄屏检查横向溢出与主行动可见性。
- 模拟 reduced-motion，并确认增强渲染失败不阻断 DOM 内容。

## 备注

- HTML-in-Canvas 仅作为视觉灵感，不采用其实验 API。
- `tree.js` 按上下文理解为 Three.js。
- 每个内聚批次独立 commit，本轮全部完成后统一 push。

## 疑问

- 无阻塞项；默认采用程序化原创几何与粒子，不引入外部 3D 资产。
