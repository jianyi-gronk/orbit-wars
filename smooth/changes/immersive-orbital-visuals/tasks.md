# 任务

## 阶段 1：增强渲染基础

- [x] **接入首页专用 Three.js 依赖** — 更新 web 包和锁文件，确保只由首页客户端组件动态加载。
- [x] **建立共享轨道世界** — 新增 `OrbitalWorld`，创建程序化星球、星场、轨道、信标与航行核心，并完整处理初始化、尺寸变化、标签页可见性和资源释放。
- [x] **连接首页状态** — 把 `activeScene`、指针位置与 reduced-motion 单向传给增强画布，保持现有 DOM 导航和 CSS 降级可用。

## 阶段 2：场景与视觉整合

- [x] **实现四幕空间状态** — 为接触、循环、网络和上行定义镜头、构图、色温与信标目标，使用阻尼过渡且不重复创建对象。
- [x] **调整视觉层级与响应式样式** — 增加画布层、暗角和局部对比度保护，降低重复 HUD 装饰，并限制移动端复杂度和横向溢出。
- [x] **完善动效与失败降级** — reduced-motion 使用静态帧，WebGL 失败保持完整 CSS 首页，点击反馈和场景导航不受影响。

## 阶段 3：验证与收尾

- [x] **补充自动化质量检查** — 覆盖动态加载、装饰层语义、可见性暂停、资源释放和 reduced-motion 分支。
- [x] **运行工程验证** — 执行 web test、lint、typecheck 与生产 build，修复本轮引入的问题。
- [x] **执行浏览器视觉验收** — 检查 `/zh`、`/en` 的桌面、窄屏、四幕切换、控制台和 reduced-motion，并记录证据。
- [ ] **按批次提交并统一推送** — 每个内聚实现批次独立 commit，所有验证通过后一次 push 到 `origin/main`。
