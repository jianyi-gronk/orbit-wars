# 任务

## 阶段 1：复现与定位

- [x] **逐项验证公开回放链路** — 检查 compact、全部 checkpoint、对象存储工件和同源代理，记录第一个真实失败点。

## 阶段 2：闭环修复

- [x] **兼容终局回放记录** — 帧重建器只处理 checkpoint/delta，并忽略最后分段中的 `result` 元数据记录。
- [x] **确认历史记录持久化约束** — 验证历史接口只关联已提交 ReplayArtifact 的比赛，worker 仍保持“上传工件后再绑定记录”。
- [x] **增强前端恢复行为** — 瞬时失败重试成功后完整恢复，分段失败提供准确阶段和错误类型而非笼统离线。

## 阶段 3：验证与交付

- [x] **补充回归测试** — 覆盖实际根因、历史暴露条件和前端恢复行为。
- [x] **验证指定旧回放** — 用 API 与浏览器确认指定 ID 可完整播放。
- [ ] **提交并统一推送** — 按内聚批次 commit，全部通过后统一 push。
