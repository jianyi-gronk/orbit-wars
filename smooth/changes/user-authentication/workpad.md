# 工作台

## 计划

- [x] 对比 Orbit Wars 当前认证骨架与“韭见”完整认证实现
- [x] 明确真实认证的用户链路、产品范围与安全边界
- [x] 创建技术设计，确定认证职责、会话格式、数据模型和迁移方式
- [x] 拆解实现任务
- [ ] 实现并验证中英双语认证闭环
- [ ] 在域名与 HTTPS 就绪后切换生产部署

## 验收标准

- 用户可以邮箱注册、登录、找回密码和退出，并返回原任务。
- 可配置显示 GitHub/Google 登录入口。
- 账户之间的舰队、策略、密钥和私有模拟严格隔离。
- 生产环境不再使用固定 `preview-commander`。
- 认证页面完整支持中英文、窄屏、键盘与安全错误反馈。

## 验证

- 单元测试覆盖密码哈希、验证码、限流、锁定、返回路径和会话失效。
- API 集成测试覆盖注册、登录、退出、重置密码和跨账户越权。
- 浏览器分别走中文、英文、移动端和登录后继续创建舰队流程。
- 部署验证检查 Secure Cookie、HTTPS、OAuth 回调和真实邮件。

## 备注

- 参考项目的产品交互和安全规则可以复用，但 SQLite + Next.js 认证代码不能直接复制到 PostgreSQL + FastAPI 架构。
- 当前 `http://47.98.155.60:4000` 只适合作为无密码预览环境。
- 选择 FastAPI/PostgreSQL 作为认证事实源；使用可撤销 opaque session，现有 OIDC JWT 校验保留为迁移兼容路径。

## 疑问

- 正式域名和 TLS 证书尚未配置，是公开真实认证上线的外部前提。
- GitHub 与 Google 凭据可以后配；对应入口应按配置自动隐藏。
