# 工作台

## 计划

- [x] 读取 Agentank 差距审计与当前代码链路
- [x] 明确产品目标、范围与验收标准
- [x] 完成技术设计
- [x] 拆解实现任务
- [x] 实现状态感知主行动
- [x] 实现首页真实回放预览与最近对局
- [x] 实现回放 Agent 分析交接
- [x] 完成工程与浏览器验证

## 验证结果

- Smooth 全仓 lint、typecheck、test 通过；165 个 Python 测试通过、5 个按环境跳过，web 47 个测试通过。
- web 生产构建通过，并已在 `http://localhost:3003` 重新启动。
- 中英文首页均加载 3 场真实公开对局，首屏 Pixi 战场为 1:1 权威回放帧。
- 桌面与窄屏均无页面级横向溢出；英文移动首屏主标题、状态行动和真实战场可读。
- 回放 Agent 交接区桌面/窄屏布局通过；受限浏览器 Clipboard API 悬空时会在 800ms 后使用兼容复制路径，实测出现 `Copied` 反馈。
- 浏览器控制台无 warning/error。

## 验收重点

- 全站入口根据用户状态只给一个明确下一步。
- 首页展示真实对局，不再用装饰雷达冒充战斗。
- 回放结束后可安全、直接交给 Agent 分析。
- 新功能完整支持中英文、窄屏和 reduced-motion。

## 约束

- Agent-only，Human 只保留未来技术口子。
- 不新增一级导航和首页第五幕。
- Agent Key 明文只在生成时出现一次，绝不进入公开回放交接包。
- 每个内聚批次及时 commit，全部完成后统一 push。

## 疑问

- 无阻塞项；本轮全部复用现有 API。
