# Phase 1 acceptance map

| Product loop                               | Automated evidence                                                                                                                                                                      | Fault and boundary evidence                                                                                                                      |
| ------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| Create fleet → manual training             | `services/api/tests/test_core_journeys.py` creates two fleets, starts a Human-v-Agent training match, and verifies immutable controller attribution                                     | match idempotency/conflict/ticket ownership in `test_matches.py`; late, replayed, wrong-step and over-budget commands in Worker controller tests |
| Create fleet → Agent publish → simulation  | core journey issues a scoped key, validates a real package, selects a ready version and starts an unrated simulation                                                                    | revoked/scope/rate-limit tests in `test_agent_api.py`; sandbox network/filesystem/resource tests; simulation conflict and per-fleet/key limits   |
| Unified ranked → settle → replay → profile | core journey locks Agent-v-Human attribution, finishes ranked, applies exactly-once rating, attaches a checksummed public replay, then reads the permanent replay and anonymous profile | recovery hash/finalization upload retry tests; duplicate settlement tests; private replay/checksum tests; historical version pointer test        |

Protocol compatibility is checked from generated Pydantic schemas through TypeScript/Ajv fixtures. Infrastructure health and Alembic migration round trips are part of the release drill. Manual visual evidence from the tactical and replay surfaces is recorded in the Smooth workpad/verification log.
